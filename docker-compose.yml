version: '3'

services:
  best_admin:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3004:3004
    container_name: best-admin
    # environment:
    #   - NODE_ENV=production
    #   - NEXT_PUBLIC_BASE_API_URL=https://v2.onnow.io/api/v1
    #   - NEXT_PUBLIC_IMAGE_URL=https://v2.onnow.io/images/



